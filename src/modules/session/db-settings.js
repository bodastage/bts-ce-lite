// const Sequelize = window.require('sequelize');
//const path = window.require('path');
//const { app, process } = window.require('@electron/remote');

export const SQLITE3_DB_NAME = 'boda-lite.sqlite3';

//let basepath = app.getAppPath();

// if (!process.env.ELECTRON_ENV === 'dev') {
//   basepath = process.resourcesPath
// } 

// const { Sequelize } = require('sequelize');

//const { sequelize, Sequelize } = require(path.join(basepath, 'src', 'db', 'models', 'index.js'));
//const { sequelize } = require('../../db/models/index.js');
// //const Sequelize = require('sequelize');

 

/*
* Path to sqlite3 database 
*/
// export const SQLITE3_DB_PATH = path.join(basepath,'src', 'db', SQLITE3_DB_NAME);

